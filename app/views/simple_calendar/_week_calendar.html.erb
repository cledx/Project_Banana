<div class="simple-calendar">
  <table class="table table-striped">
    <% date_range.each_slice(7) do |week| %>
      <thead>
        <tr>
          <% week.each do |day| %>
            <% if params[:id] %>
              <th class="text-center <%= 'today' if day == Date.current %>">
                <% if day >= @week.days.first.date.to_date && day <= @week.days.last.date.to_date %>
                  <%= link_to(day.strftime("%a %d"), week_day_path(@week, day), class: "text-decoration-none") %>
                <% else %>
                  <%= link_to(day.strftime("%a %d"), week_day_path(@week.next_week, day), class: "text-decoration-none") %>
                <% end %>
              </th>
            <% elsif on_weeks_new? %>
              <th class="text-center <%= 'today' if day == Date.current %>"><%= day.strftime("%A") %></th>
            <% else %>
              <th class="text-center <%= 'today' if day == Date.current %>"><%= day.strftime("%a %d") %></th>
            <% end %>
          <% end %>
        </tr>
      </thead>

      <tbody>
        <tr class="text-center">
          <% week.each do |day| %>
            <%= content_tag :td, class: calendar.td_classes_for(day) do %>
              <% instance_exec(day, calendar.sorted_events_for(day), &passed_block) %>
            <% end %>
          <% end %>
        </tr>
      </tbody>
    <% end %>
  </table>
</div>
